{{gh-token-input
    options=this.availableExperts
    selected=this.selectedExperts
    onchange=(action "updateExperts")
    allowCreation=false
    renderInPlace=true
    triggerId=this.triggerId
}}
